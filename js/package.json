{
  "name": "userlens-sdk",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "node",
    "web"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@rollup/plugin-terser": "^0.4.4",
    "rollup-plugin-terser": "^7.0.2"
  }
}
